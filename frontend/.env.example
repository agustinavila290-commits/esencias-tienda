VITE_API_URL=http://localhost:8001/api
VITE_WHATSAPP_NUMBER=549XXXXXXXXXX
VITE_INSTAGRAM_URL=

# URL pública real del sitio (sin barra final) — se usa para canonical, Open
# Graph, Twitter Card y JSON-LD. En producción: https://esencias.avilamotorepuesto.com.ar
VITE_SITE_URL=http://localhost:5174

# Google OAuth — Login con Google
# Copiar el mismo Client ID que pusiste en backend/.env
# Ir a Google Cloud Console → Credentials → OAuth 2.0 Client ID
VITE_GOOGLE_CLIENT_ID=
